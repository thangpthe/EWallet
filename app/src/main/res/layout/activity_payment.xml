<!--
  ════════════════════════════════════════════════════════════
  HƯỚNG DẪN: Chèn đoạn XML dưới đây vào activity_payment.xml
  NGAY SAU thẻ đóng của "cardVoucher" MaterialCardView
  và TRƯỚC "Summary Card"
  ════════════════════════════════════════════════════════════
-->

<!-- ── Payment Source Section ── -->
<com.google.android.material.card.MaterialCardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="12dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="#EEF0FF"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:text="💳  Phương thức thanh toán"
            android:textColor="#0D0F3C"
            android:textSize="14sp"
            android:textStyle="bold"/>

        <!-- Source toggle: Ví | Visa -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="2">

            <!-- Tab Ví điện tử -->
            <androidx.cardview.widget.CardView
                android:id="@+id/tabPayWallet"
                android:layout_width="0dp"
                android:layout_height="52dp"
                android:layout_weight="1"
                android:layout_marginEnd="6dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="#3D5AFE">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:orientation="horizontal">
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="👛  "
                        android:textSize="15sp"/>
                    <TextView
                        android:id="@+id/tvTabPayWallet"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Ví điện tử"
                        android:textColor="#FFFFFF"
                        android:textSize="13sp"
                        android:textStyle="bold"/>
                </LinearLayout>
            </androidx.cardview.widget.CardView>

            <!-- Tab Thẻ Visa -->
            <androidx.cardview.widget.CardView
                android:id="@+id/tabPayVisa"
                android:layout_width="0dp"
                android:layout_height="52dp"
                android:layout_weight="1"
                android:layout_marginStart="6dp"
                app:cardCornerRadius="12dp"
                app:cardElevation="0dp"
                app:cardBackgroundColor="#EEF0FF">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:orientation="horizontal">
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="💳  "
                        android:textSize="15sp"/>
                    <TextView
                        android:id="@+id/tvTabPayVisa"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="Thẻ Visa"
                        android:textColor="#5063C8"
                        android:textSize="13sp"
                        android:textStyle="bold"/>
                </LinearLayout>
            </androidx.cardview.widget.CardView>
        </LinearLayout>

        <!-- Visa card detail (shown when Visa selected) -->
        <LinearLayout
            android:id="@+id/layoutVisaInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="12dp"
            android:visibility="gone">

            <!-- Selected card display -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="#F5F6FF"
                android:padding="12dp"
                android:gravity="center_vertical">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="💳"
                    android:textSize="24sp"/>

                <TextView
                    android:id="@+id/tvSelectedCard"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="12dp"
                    android:text="Chưa chọn thẻ"
                    android:textColor="#0D0F3C"
                    android:textSize="13sp"
                    android:textStyle="bold"/>

                <Button
                    android:id="@+id/btnChangeCard"
                    android:layout_width="wrap_content"
                    android:layout_height="32dp"
                    android:text="Đổi thẻ"
                    android:textSize="11sp"
                    android:textColor="#3D5AFE"
                    android:backgroundTint="@android:color/transparent"
                    android:padding="0dp"
                    style="@style/Widget.MaterialComponents.Button.TextButton"/>
            </LinearLayout>

            <!-- Manage cards link -->
            <Button
                android:id="@+id/btnManageCards"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="+ Quản lý thẻ của tôi"
                android:textSize="12sp"
                android:textColor="#3D5AFE"
                android:backgroundTint="@android:color/transparent"
                android:padding="0dp"
                android:layout_marginTop="4dp"
                style="@style/Widget.MaterialComponents.Button.TextButton"/>

        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>